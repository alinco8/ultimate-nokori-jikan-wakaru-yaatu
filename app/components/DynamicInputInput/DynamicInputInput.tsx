import { AutoScaledInput } from '~/components/AutoScaledInput';
import styles from './style.module.scss';

export const DynamicInputInput = () => {
    return <AutoScaledInput className={styles.dynamicInputInput} />;
};
